name: CI

on:
	push:
		branches: [main]
		

jobs:
	integrationTest:
		run-on: ubuntu-latest
		steps:
			- uses: actions/checkout@v2
			- run: npm install 
			- run: npm start &
			- run: npx newman run test/arms-sales-statsAPI-TEST.json