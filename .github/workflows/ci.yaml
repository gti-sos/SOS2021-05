name: CI


on:
  push:
    branches: [ main ]

jobs:
  integrationTests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm start &
	  - run: npm newman run test/attacks_statsAPI_TEST
      - run: npx newman run test/arms-sales-statsAPI-TEST.json